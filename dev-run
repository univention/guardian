#!/usr/bin/bash

mkdir -p management_service_dir/
USER_ID=$(id --user) GITLAB_IP=$(getent -i ahostsv4 git.knut.univention.de | head -n 1 | cut -d ' ' -f 1) exec docker compose -f dev-compose.yaml up --build
