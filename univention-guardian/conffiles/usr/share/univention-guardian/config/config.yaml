@%@UCRWARNING=# @%@
# SPDX-FileCopyrightText: 2026 Univention GmbH
# SPDX-License-Identifier: AGPL-3.0-only

# https://docs.cerbos.dev/cerbos/latest/configuration/index.html

server:
  # Docker-compose binds this to 127.0.0.1 on the host.
  httpListenAddr: ":3592"
  playgroundEnabled: false
  apiExplorerEnabled: false

storage:
  driver: "disk"
  disk:
    directory: "/policies"
    watchForChanges: true

@!@
if configRegistry.is_true('guardian/authentication', False):
    jwks_url = configRegistry.get('guardian/cerbos/keycloak/jwks-url', '')
    # Re-verify the bearer the proxy forwarded so claims are usable in
    # policies as request.auxData.jwt.claims.* (defense-in-depth).
    print("auxData:")
    print("  jwt:")
    print("    keySets:")
    print("      - id: keycloak")
    print("        remote:")
    print("          url: \"%s\"" % jwks_url)
@!@
